import numpy as np
import tensorflow as tf

def get_data(file_directory):
	"""
	This is where we should load the data from the file and preprocess it.
	
	Typically in DL we'd also split it into training and testing examples.
	So we'd return something like 4 numpy arrays:
		return train_inputs, train_outputs, test_inputs, test_output
	"""
